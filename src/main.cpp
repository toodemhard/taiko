#include "app.h"

// #include "font.h"
// #include "stb_image_write.h"

int main() {
    // constexpr int ft_width = 512, ft_height = 512;
    // constexpr int ft_length = ft_width * ft_height;
    // stbi_write_png("main.png", ft_width, ft_height, 1, main_atlas().data(), 1 * ft_width);
    return app::run();
}
